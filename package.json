{
  "name": "header",
  "private": true,
  "version": "1.2.12",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --parallel --stream --",
    "dev": "lerna run dev --parallel --stream -- --watch",
    "build": "lerna run build --",
    "clean": "lerna run clean"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
